export default function viewProfileHandlerFromUtils(
  tab,
  filter,
  role,
  user_id
) {
  console.log({ tab, filter, role, user_id });

  switch (key) {
    case value:
      break;

    default:
      break;
  }
}
